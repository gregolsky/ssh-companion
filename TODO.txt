-allow --instructions-loop to accept a file path (e.g. @file) for longer prompts
-fix windows mode
-extend test/smoke-test.sh: drive companion.sh non-interactively, assert .mcp.json entry is added, MCP server is callable via docker exec, prune cleans up the entry
-add test run to gh actions (tests.yml + smoke-test.yml already exist; clarify what's missing — coverage? matrix? Windows runner?)
